management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: '/724d72d1171b/actuator'
  endpoint:
    health:
      show-details: ALWAYS
    shutdown:
      enabled: true

spring:
  datasource:
    url: jdbc:mysql://47.99.194.77:3306/huikesysdevdb?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&serverTimezone=Asia/Shanghai
    username: huikedev
    password: Qwer^#@90
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 139.224.106.219:8848
        namespace: f90801c5-5869-4ec2-868d-5b0a87e13fbe

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 30000
  threadpool:
    default:
      coreSize: 10
      maximumSize: 10
      maxQueueSize: -1

ribbon:
  okhttp:
    enabled: true
  #超时设置，如果路由方式是serviceId，该超时设置将生效
  ReadTimeout: 30000
  ConnectTimeout: 30000
  MaxAutoRetries: 0
  MaxAutoRetriesNextServer: 1

#bot redis
cmf:
  redis:
    servers: 120.26.38.9:6379
    password: qwerASDF
    #保留时间30分钟，默认30分钟
    expireTime: 1800
    cacheName: huike_redis_single

#mybatis
mybatis:
  type-aliases-package: tk.mybatis.springboot.model
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #打印sql语句,调试用
#pagehelper
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

logging: 
  level: 
    root: info
