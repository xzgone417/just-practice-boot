<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.egrid.ai.talk.dao.ITalkDao" >

	<update id="updateReplyContent">
	    update
			t_dm_talk_cycles_record t
		set
			t.talk_resp = #{talkResp},
			t.updated_by = #{updatedBy},
			t.updated_date = #{updatedDate}
		where
			t.talk_record_id = #{talkRecordId}
	</update>
	
	
	<update id="updateTalkInter">
		update
			t_dm_talk_inter_result t
		set
			t.talk_request = #{talkRequest},
			t.talk_response = #{talkResponse},
			t.updated_by = #{updatedBy},
			t.updated_date = #{updatedDate}
		where
			t.talk_record_id = #{talkRecordId}
	</update>
</mapper>